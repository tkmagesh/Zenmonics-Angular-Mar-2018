import { Component } from '@angular/core';

import * as angCore from '@angular/core';

console.dir(angCore);

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'app';
}
